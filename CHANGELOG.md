# 更新日志

本项目的所有显著变更均记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（主版本.次版本.修订号）。

---

## [1.3.2] - 2026-09-19

### 修复

- **BGM/SE 不播放**：`createAiotAdapter` 的 `resourceManager` 闭包变量在 game.ux 调用时未注入（始终为 `undefined`），导致 `audio.play()` 首行 `!resourceManager` 直接 return，剧本节点 8（音乐）发出的播放请求全部静默丢弃。改为 `readConfig` 成功后自动初始化 `ResourceManager`，音频 URI 解析恢复正常。
- **game.ux 重复属性**：`private` 块中 `isSkipping` 声明两次（引擎状态区与 UI 状态区各一份），后者覆盖前者，删除冗余声明。

### 其他

- **测试脚本**：`run-tests.mjs` 的引擎文件复制逻辑增加 import 路径修正（`./xxx.js` → `./xxx.mjs`），与 `regression-real.mjs` 对齐，避免新增跨模块 import 后测试报 `MODULE_NOT_FOUND`。
- 版本号 `versionCode 9 → 10`，`versionName 1.3.0 → 1.3.2`。

---

## [1.3.0] - 2026-09-18

### 新增

- **主页「继续游戏」按钮**：置顶显示，存在自动存档时点击直接读自动存档进入游戏；无存档提示「暂无自动存档」。「开始游戏」改名为「重开」，保持原有从头开始逻辑。
- **自动保存（实时 + 节流）**：
  - 新增独立存档键 `auto_save_data`，自动存档槽在存档页**置顶**显示，为自动专用（只读，不可手动覆盖、不可长按删除）。
  - 游戏内每出现一句新对话即写入自动存档，写入频率节流为 **3 秒最多一次**，兼顾实时性与存储寿命。
  - 自动保存可在设置页开关（默认开）。
  - 从「载入进度」页**手动读档**进入游戏时自动保存自动停用（避免覆盖自动槽），仍靠手动保存；从自动存档槽读取则继续自动保存。
  - 从游戏「返回主页」退出阅读时，若自动保存开启则强制写入一次并提示「进度已自动保存」。
- **自动播放**（游戏内菜单开启）：整句完整显示后等待 **0.5 秒**自动进入下一句；手动点击翻句时立即响应并重置计时；遇到选项时暂停等待选择。菜单中显示当前开关状态（自动播放：开/关）。
- **屏幕常亮**：设置页新增独立开关（默认关）；**自动播放开启时自动同步启用屏幕常亮**，自动播放关闭且独立开关关闭时恢复熄灭。离开阅读页时强制关闭常亮。
- **纯黑模式**（设置页开关，默认关）：开启后主页与阅读界面**均不渲染任何图片**，底色纯黑；页面布局自动适配屏幕，不受图片原始尺寸约束。
- **振动反馈**（设置页开关，默认开）：主页、阅读界面、选项、菜单、设置、存档、关于页等所有点击操作触发短振动。采用 `@system.vibrator.vibrate({mode:'short'})`，设备不支持时静默失败。
- **上一句按钮**：说话人框右侧新增「上一句」按钮（样式与顶部控制按钮同款构造），点击回到上一条对话；已在第一句时提示「已是第一句」。自动播放状态下回退会重置计时。
- **阅读界面底部居中时间显示**：字号与人名一致（15px），每 30 秒自动刷新。

### 修改

- **文字框扩大至 1.25 倍**：对话窗高度 120px → **150px**；说话人框随之上移（bottom 160 → 190px）；左右边距 8px → **12px**；内边距 10px → **14px**。
- **字号适当增大**：默认字号 18px → **22px**；字号滑块上限 24 → **30px**。
- **文字加粗开关**（设置页，默认开）：阅读正文按开关动态加粗。
- **阅读菜单选项字号 1.25 倍**：菜单按钮文字 15px → **19px**，按钮高度 46 → 50px，菜单容器改为可滚动。
- **主页字号 1.25 倍并加粗**：按钮文字 17px → **21px**（加粗原有保留）。
- **关于页**：
  - 应用名改为「魔女的夜宴 **Band 9/10/11**」；
  - 作者行适配标识 `hrk666666` → **`hrk_`**，适配范围标注 9/10/11；
  - 上游仓库名缩短（去掉 `github.com/` 前缀）；
  - 作者行之后的说明内容字号 **1.3 倍**（label 13→17px，内容 14→18px）；
  - 单独新增一项「**返回载入进度页**」，点击直达存档页。
- **系统设置页**：「保存」「退出」按钮改为**居中并排**，间距缩小。
- **快进按钮尺寸 1.25 倍**：内边距 8/16px → **10/20px**，文字 14px → **18px**。

### 修复

- **长按误触快进**：快进按钮弃用系统 `longpress`，改为自定义触摸计时（按压 **600ms** 且无位移才触发），移动立即取消并停止快进；快进按钮触摸不再冒泡触发菜单。菜单长按同样改为 600ms 阈值，且长按唤出菜单后吞掉随后的误点击，避免同时翻句。
- **自动播放/快进互不干扰**：两者独立工作，手动操作优先。

### 其他

- **版本号**：`versionName 1.2.1 → 1.3.0`，`versionCode 7 → 8`（package.json 同步 1.3.0）。
- **manifest**：新增 `system.brightness`、`system.vibrator` 能力声明。
- **签名**：新增 `sign/` 证书（RSA-2048，有效期 10 年，CN=SanobaWitch），`aiot release` 生产包使用该固定签名；`.gitignore` 不再忽略 `sign/`，签名入库供 GitHub Actions 复用，保证后续版本签名一致、可覆盖安装。
- **CI**：新增 GitHub Actions workflow（`.github/workflows/build.yml`），推送 `main` 后自动 `npm ci && npm run release`，RPK 产物以 workflow artifact 形式提供（不创建 GitHub Release）。

### 注意

- 本版本 RPK 使用**全新签名证书**，与 1.2.1 本地构建的签名不同。若覆盖安装旧版时提示签名冲突，需先卸载旧版再安装（存档会随卸载清除，建议先备份）。

---

## [1.2.1] - 2026-09-XX

- 同步上游 1.1.2+5 全部功能：长按快进、EV/SD 事件 CG、隐藏 UI、引擎模块化（scriptEngine.js）、剧本新格式、菜单时间显示。
- 手环 9/10 窄屏适配：designWidth 212、对话框 120px、字号默认 18、首页 flex 自适应布局。
- 修复：长按菜单不再依赖 DEBUG、快进遇选项说话人显示错误、首页 prompt 未导入、标题图改用 jpg。

## [1.1.2+5] - 上游（9Pro 版，已存档）

- 上游 futrw4v/Sanoba-Witch-MiBand-9Pro 最终版，本仓库功能基底。
