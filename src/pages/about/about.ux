<template>
  <div class="page" @swipe="swipeBack" @longpress="longPressBack">
    <image class="bg-image" if="{{ !settings.darkMode }}" src="/common/title_bg.jpg"></image>

    <!-- 顶部标题 -->
    <div class="header">
      <text class="header-title">关于</text>
    </div>

    <scroll scroll-y="true" class="list">
      <!-- 应用信息卡片 -->
      <div class="card">
        <div class="app-row">
          <image class="app-icon" if="{{ !settings.darkMode }}" src="/common/logo.png"></image>
          <div class="app-info">
            <text class="app-name">魔女的夜宴</text>
            <text class="app-version">Band 9/10/11 · v{{ appVersion }}</text>
          </div>
        </div>
      </div>

      <!-- 信息列表 -->
      <div class="card">
        <text class="info-label">作者</text>
        <text class="info-value">futrw4v（原版）/ hrk_（适配）</text>
      </div>
      <div class="card">
        <text class="info-label">上游仓库</text>
        <text class="info-value">Sanoba-Witch-MiBand-9Pro</text>
      </div>
      <div class="card">
        <text class="info-label">协议</text>
        <text class="info-value">GNU GPL v3.0</text>
      </div>
      <div class="card">
        <text class="info-label">说明</text>
        <text class="info-value">请勿用于商业用途</text>
      </div>

      <!-- 返回存档 -->
      <div class="card card-action" @click="routeLoad">
        <text class="info-label">返回载入进度页</text>
        <image class="arrow" src="/common/images/enter.png"></image>
      </div>
    </scroll>
  </div>
</template>

<script>
import router from "@system.router"
import manifest from "../../manifest.json"
import {DEBUG} from "../../common/constants.js"
import {vibrateShort} from "../../common/vibrate.js"

export default {
  private: {
    appVersion: ""
  },

  onInit() {
    this.appVersion = manifest.versionName
  },

  swipeBack() {
    vibrateShort()
    router.back()
  },

  longPressBack() {
    if (DEBUG) router.back()
  },

  routeLoad() {
    vibrateShort()
    router.push({uri: "pages/data"})
  }
}
</script>

<style>
.page {
  flex-direction: column;
  background-color: #000000;
  width: 100%;
  height: 100%;
}

.bg-image {
  position: absolute;
  width: 100%;
  height: 100%;
  opacity: 0.15;
}

.header {
  position: absolute;
  top: 20px;
  width: 100%;
  height: 44px;
  justify-content: center;
  align-items: center;
}

.header-title {
  font-size: 26px;
  font-weight: bold;
  color: #ffffff;
}

.list {
  position: absolute;
  top: 76px;
  left: 12px;
  right: 12px;
  bottom: 16px;
  flex-direction: column;
}

.card {
  width: 100%;
  margin-bottom: 8px;
  padding: 14px 18px;
  background-color: #262626;
  border-radius: 20px;
  flex-direction: column;
}

.card:active {
  background-color: #333333;
}

.app-row {
  flex-direction: row;
  align-items: center;
}

.app-icon {
  width: 48px;
  height: 48px;
  border-radius: 24px;
  margin-right: 14px;
}

.app-info {
  flex: 1;
  flex-direction: column;
}

.app-name {
  font-size: 22px;
  font-weight: bold;
  color: #ffffff;
}

.app-version {
  font-size: 16px;
  color: rgba(255, 255, 255, 0.6);
  margin-top: 2px;
}

.info-label {
  font-size: 17px;
  color: rgba(255, 255, 255, 0.5);
  margin-bottom: 4px;
}

.info-value {
  font-size: 19px;
  color: #ffffff;
}

.card-action {
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
}

.arrow {
  width: 24px;
  height: 24px;
}
</style>
